{
  "name": "nora-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nora-test",
    "create-local": "graph create --node http://localhost:8020/ nora-test",
    "remove-local": "graph remove --node http://localhost:8020/ nora-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nora-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
